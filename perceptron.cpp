#include "perceptron.h"

bool perceptron::isactive(){
	return true;
}

void Network::resizeLayers(int numLayers, int seed){
	srand(seed);
	for(int i = 0 ; i < numLayers-1; ++i){
		for( int j = 0; j < 784; ++j){
		network[i]
	}
}
